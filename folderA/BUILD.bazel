load("@npm//@bazel/typescript:index.bzl", "ts_config", "ts_project")

ts_config(
    name = "config",
    src = "tsconfig.json",
    deps = [
        "//:tsconfig-base",
    ],
)

ts_project(
    composite = True,
    tsconfig = "config",
    visibility = ["//visibility:public"],
)
